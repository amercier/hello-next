{
  "name": "hello-next",
  "private": true,
  "main": "development-server.js",
  "baseUrl": "/hello-next",
  "scripts": {
    "dev": "node .",
    "build": "next build",
    "export": "next export -o dist/",
    "deploy": "touch dist/.nojekyll && gh-pages --dotfiles --dist dist/"
  },
  "dependencies": {
    "express": "^4.16.4",
    "isomorphic-unfetch": "^3.0.0",
    "next": "^7.0.2",
    "react": "^16.6.3",
    "react-dom": "^16.6.3"
  },
  "devDependencies": {
    "babel-plugin-transform-define": "^1.3.0",
    "gh-pages": "^2.0.1"
  }
}
